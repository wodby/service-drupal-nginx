ARG WODBY_BASE_IMAGE
FROM ${WODBY_BASE_IMAGE}

ENV NGINX_VHOST_PRESET drupal11

ARG COPY_FROM
ARG COPY_TO
COPY --chown=wodby:wodby ${COPY_FROM}/web ${COPY_TO}/web

ARG DOCROOT_SUBDIR
ARG DRUPAL_SITE

RUN make init -f /usr/local/bin/actions.mk
